function add(a, b) {
  var number = a + b;
  return number;
}

function subtract(a, b) {
  var number = a - b;
  return number;
}

function multiply(a, b) {
  var number = a * b;
  return number;
}

function divide(a, b) {
  var number = a / b;
  return number;
}

function inc(a) {
  var increase = a + 1;
  return increase;
}

function dec(a) {
  var decrease = a - 1;
  return decrease;
}

function makeInt(n) {
  
}